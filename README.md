# 🤖 TrixBot - Каталог услуг Будапешта

Модульный Telegram бот для управления каталогом услуг и системой рейтингов.

## 📋 Функционал

### Система каталога (5 слотов)
- **Slot 1-2**: Обычные услуги
- **Slot 3**: TopGirls/TopBoys (случайный выбор)
- **Slot 4**: Приоритетные посты/Реклама
- **Slot 5**: Специальный слот (/setslot)

### Команды пользователей
- `/start` - Регистрация с уникальным UID
- `/catalog` - Просмотр каталога услуг
- `/search` - Поиск по тегам
- `/review` - Оставить отзыв (кулдаун 1 час)
- `/gorateme` - Подать заявку в ТОП (кулдаун 3 часа)
- `/toppeople` `/topboys` `/topgirls` - Рейтинги

### Админ функции
- `/admin` - Админ панель
- `/stats` - Статистика бота
- `/setslot` - Установка специального слота
- `/moderation` - Модерация контента
- `/changeuid` - Изменение UID пользователя

## 🏗️ Архитектура

```
trixbot/
├── core/               # Ядро бота
│   ├── bot.py         # Инициализация бота
│   ├── config.py      # Конфигурация
│   ├── dispatcher.py  # Диспетчер
│   └── states.py      # FSM состояния
│
├── database/          # База данных
│   ├── base.py       # Базовые классы
│   └── models/       # Модели
│       ├── user.py
│       ├── catalog.py
│       ├── rating.py
│       ├── cooldown.py
│       └── special_slot.py
│
├── handlers/          # Обработчики
│   ├── commands/     # Команды
│   ├── callbacks/    # Inline кнопки
│   └── messages/     # Сообщения
│
├── services/         # Бизнес-логика
│   ├── user_service.py
│   ├── catalog_service.py
│   ├── rating_service.py
│   ├── cooldown_service.py
│   └── admin_service.py
│
├── keyboards/        # Клавиатуры
│   ├── reply.py
│   └── inline.py
│
├── managers/         # Менеджеры процессов
│   ├── catalog_manager.py
│   └── rating_manager.py
│
├── middleware/       # Промежуточные обработчики
│   ├── logging_middleware.py
│   ├── throttling_middleware.py
│   └── user_middleware.py
│
└── main.py          # Точка входа
```

## 🚀 Установка

1. Клонируйте репозиторий
2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env`:
```env
BOT_TOKEN=your_bot_token
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/trixbot
ADMIN_IDS=123456789,987654321
REDIS_URL=redis://localhost:6379

# Каналы
CATALOG_ID=-1002601716810
ZAYAVKI_ID=-1002734837434
# ... остальные ID каналов
```

5. Запустите бота:
```bash
python main.py
```

## 🔧 Технологии

- Python 3.11+
- aiogram 3.13
- SQLAlchemy 2.0 (async)
- PostgreSQL
- Redis (для кэша)
- Loguru (логирование)

## 📊 База данных

### Основные таблицы:
- `users` - Пользователи с уникальными UID
- `catalog_posts` - Карточки каталога
- `catalog_reviews` - Отзывы
- `rating_posts` - Рейтинговые посты
- `rating_votes` - Голоса
- `cooldowns` - Кулдауны команд
- `special_slots` - Специальные слоты

## 🆔 Система UID

Каждому пользователю присваивается уникальный UID (1-99999).
Зарезервированные номера (не могут быть присвоены автоматически):
- Красивые числа: 1, 2, 3, 5, 7, 777, 999, 1234
- Технические: 404, 500, 8080
- Специальные: 42, 69, 420, 666

## 📝 Процесс /gorateme

1. **Имя** - как вас зовут
2. **Профиль** - ссылка на Instagram/Telegram
3. **О себе** - 3 слова (макс 7 символов каждое)
4. **Пол** - для категории TopGirls/TopBoys
5. **Медиа** - фото или видео

После модерации пользователи могут голосовать от -2 до +2.

## 👨‍💻 Разработка

Made with ♥️ for Budapest
