# 🚂 Инструкция по развертыванию на Railway

## Шаг 1: Подготовка репозитория

1. Создайте репозиторий на GitHub
2. Загрузите туда весь проект trix-bot

```bash
git init
git add .
git commit -m "Initial commit: TrixBot structure"
git branch -M main
git remote add origin your-repo-url
git push -u origin main
```

## Шаг 2: Создание проекта на Railway

1. Откройте [Railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий `trix-bot`

## Шаг 3: Добавление PostgreSQL

1. В вашем проекте нажмите **"New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст переменную `DATABASE_URL`
4. Эта переменная автоматически будет доступна вашему боту

## Шаг 4: Настройка переменных окружения

В настройках вашего сервиса (service settings) → вкладка **"Variables"** добавьте:

```env
BOT_TOKEN=7251773537:AAFLIzC4jj6m31y6aalZPi3BbK-zxU2nHRk

# Каналы
MARKET_ID=-1003033694255
BPMAIN_ID=-1002743668534
BPCHAT_ID=-1002883770818
CATALOG_ID=-1002601716810
PARTNERS_ID=-1002919380244
ASOCIAL_ID=-1003088023508
BATADAZE_ID=-1003114019170

# Админ чаты
ZAYAVKI_ID=-1002734837434
erranncom_ID=-1003039151203
statification_ID=-4843909295

# Админы (ваши Telegram ID через запятую)
ADMIN_IDS=123456789,987654321

# Окружение
ENVIRONMENT=production
DEBUG=False
```

⚠️ **ВАЖНО:** `DATABASE_URL` создается автоматически при добавлении PostgreSQL!

## Шаг 5: Настройка билда (опционально)

Railway автоматически определит Python проект и установит зависимости из `requirements.txt`.

Если нужно кастомизировать:
1. Settings → Build
2. Build Command: `pip install -r requirements.txt`
3. Start Command: `python main.py`

## Шаг 6: Деплой

1. Railway автоматически начнет деплой
2. Следите за логами во вкладке **"Deployments"**
3. Успешный деплой покажет: `✅ Бот запущен: @Trixlivebot`

## Шаг 7: Проверка работы

1. Откройте Telegram
2. Найдите @Trixlivebot
3. Отправьте команду `/start`
4. Вы должны получить приветственное сообщение с вашим UID

## 📊 Мониторинг

### Просмотр логов
- Railway → Ваш проект → Service → вкладка "Logs"
- Логи обновляются в реальном времени

### Метрики
- Railway → Ваш проект → Service → вкладка "Metrics"
- Отслеживайте CPU, RAM, Network

### Health Check
```bash
# Локально (для теста)
python DEPLOY/health_check.py
```

## 🔧 Обновление бота

```bash
# Внесите изменения в код
git add .
git commit -m "Update: описание изменений"
git push

# Railway автоматически задеплоит новую версию
```

## ⚠️ Troubleshooting

### Бот не запускается

1. Проверьте логи в Railway
2. Убедитесь что все переменные окружения установлены
3. Проверьте что `DATABASE_URL` создан
4. Проверьте формат `BOT_TOKEN`

### Ошибки БД

1. Убедитесь что PostgreSQL добавлен
2. Проверьте `DATABASE_URL` в переменных
3. Формат должен быть: `postgresql+asyncpg://...`

### Бот не отвечает

1. Проверьте логи - возможны ошибки
2. Убедитесь что токен правильный
3. Проверьте что бот не заблокирован Telegram

## 📝 Полезные команды Railway CLI

```bash
# Установка Railway CLI
npm i -g @railway/cli

# Логин
railway login

# Просмотр логов
railway logs

# Подключение к PostgreSQL
railway psql

# Просмотр переменных
railway variables
```

## 🎉 Готово!

Ваш TrixBot теперь работает на Railway 24/7!

Основные преимущества Railway:
- ✅ Автоматический деплой при push в GitHub
- ✅ Бесплатный план с достаточными лимитами
- ✅ Встроенная PostgreSQL
- ✅ Простой мониторинг и логи
- ✅ Легкое масштабирование

---

💡 **Совет:** Добавьте webhook для уведомлений о деплое в ваш Telegram чат!
